工具升级：
8. read二进制文件先Markitdown从prompt写进工程
现状：当前二进制文件读取能力有限。
期望：集成Markitdown等工具，支持将二进制文件（如PDF、PPT等）转换为文本格式。将二进制文件真正自动转成Markdown文件件非用prompt引导agent用markitdown解读——直接读图转base64，PDF，PPT转markdown，表格分sheet页转csv

性能提升：
47. new agent loop start有时等很久才出现
现状：new agent loop start（新代理循环启动）有时等很久才出现，影响用户体验。按照原设计，
问题：启动延迟可能由多种原因造成，如模型加载、配置初始化、资源分配等。
期望：优化agent loop启动性能，减少启动时间，提供异步加载等机制。
12. 会话列表刷新性能
现状：`refresh_sessions_index_from_disk`函数在频繁创建/删除会话时存在I/O瓶颈，需要遍历整个sessions目录。
期望：优化索引加载机制，减少磁盘I/O，提高会话列表刷新响应速度。
32. 启动时列表会话太多响应慢的问题
现状：启动时需要加载所有会话信息，会话数量多时响应缓慢。
期望：优化启动加载机制，支持分页加载或懒加载会话列表。
48. 发送消息点击发送后要很久才能发出去
现状：有时候发送一条消息点击了发送按钮后要等待很长时间才能实际发送出去。
问题：发送延迟可能由前端处理、网络请求、后端验证等原因造成。
期望：优化消息发送流程，减少发送延迟，提供发送状态反馈或异步发送机制。
6. Subagent小窗分批渲染
现状：前端subagent小窗打开后，一次性渲染该subagent的全部`ui_events`。如果subagent跑了50轮，小窗里就是几百条事件，加载卡顿。
期望：默认只渲染最后的final结果，往上滚动时分批加载，类似主聊天区的"更早N轮对话"（初步设为3轮）按钮机制。
29. 改写发送时响应也很慢
现状：改写功能发送时响应缓慢。
期望：优化改写功能的响应速度。

前端BUG：
3. 文件选择器功能
现状：文件选择器功能已修复路径按钮不生效的问题，但多选文件功能尚未实现。
问题：在有的电脑上点不开文件选择的窗口。
期望：修复文件选择器兼容性问题，确保在所有电脑上都能正常打开。
13. 前端文件目录点击可打开的转置与索引，哪些格式能点击打开
现状：前端支持点击打开工作区文件，通过`/api/open-workspace-file`端点实现。
期望：明确支持的文件格式列表，并提供更好的文件打开体验。
15. "在本机打开（工作区文件）"的文本描述
现状：前端显示"在本机打开（工作区文件）"的提示文本。
期望：优化文本描述，使其更清晰易懂。
35. 子agent转全屏时会窗口会闪一下
现状：切换到全量显示时界面会闪烁。
期望：优化切换动画，避免界面闪烁。
40. 浅色版本的滚动条hover颜色无变化
现状：浅色主题下，滚动条hover状态无颜色变化。
期望：修复滚动条hover样式，提供更好的视觉反馈。
44. 生成完直接分支会弹窗告警，让等1-2秒，但是不切会话就会一直告警，切了会话立即没
现状：从代码分析看，`waitForBranchFinalPersisted`函数会等待2.6秒检查最终回答是否已持久化。如果未持久化，会显示"分支稍后再试"的告警。但如果不切换会话，告警会一直显示。
期望：优化分支告警机制，确保告警在适当时间后自动消失，或提供更好的用户交互体验。
45. 有查看输出的subagent删不掉（or过一会再点才能删除）
现状：从代码分析看，subagent删除功能需要先中断任务、取消注册、清理工作树等操作。有"查看输出"功能的subagent可能因为输出面板处于打开状态或正在加载中，导致删除操作被阻塞。或者可能存在某种资源锁定机制，需要等待一段时间才能删除。
期望：优化subagent删除逻辑，确保即使有"查看输出"功能的subagent也能被正常删除，或提供明确的删除失败原因。可以考虑在删除前自动关闭输出面板，或提供强制删除选项


后端BUG：
26. 前端自动续接：无法判断在上一轮回答中是否已经收到了子agent的结果
现状：自动续接功能无法准确判断子agent结果是否已被读取，导致不管如何都有自动续接的提示。
期望：改进自动续接判断逻辑，只有在子agent已完成且结果未被读取时才提示续接。
17. 重试校验失效？为什么会无限重复
现状：从会话`d704b84b-24bc-40c4-af40-d67b5fb290b1`可以看到，agent执行了50轮ReAct循环，每轮都在创建类似的文件（验证报告、总结等），形成了无限重复。
期望：修复重试校验机制；添加循环检测，避免agent陷入无限重复
39. 首条agent loop start响应慢
现状：首次启动agent循环时响应较慢。
期望：优化启动性能，减少首次响应延迟。

压缩/记忆模块：
18. 首次压缩解析错误是否重试？第二次压缩为什么直接显示轮次用尽？自动续接功能不能识别subagent结果是否已经被读取过
现状：压缩过程中存在错误处理不完善的问题。
期望：
1.	首次压缩解析错误时应支持重试
2.	第二次压缩不应直接显示轮次用尽
3.	自动续接功能应能识别subagent结果是否已被读取
14. react轮次增加一个无限设置
现状：ReAct循环有最大迭代次数限制（默认100次，由`MAX_REACT_ITER`控制）。
期望：增加"无限"设置选项，允许用户禁用迭代次数限制（需谨慎使用）。
31. 优化分支改写备份恢复功能
现状：分支、改写、备份、恢复功能可能存在性能或功能问题。
期望：全面优化这些功能，提高用户体验。


安全：
4. ls({'path': ...})等调试信息泄露
现状：日志中提到的类似调试字符串在后端核心逻辑中被过滤。
期望：检查前端是否有未清理的调试代码，确保不会泄露敏感信息。
41. 如何将当前的API URL和KEY加密，让所有人无法获取
现状：API URL和KEY可能以明文形式存储在`.env`文件中。
期望：实现API配置的加密存储，防止未授权访问。

