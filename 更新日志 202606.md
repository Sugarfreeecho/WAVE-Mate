# 更新日志 · 2026年6月

> 自动生成于 2026-06-07 09:21:06
> 数据来源: Codex 会话记录 (`C:\Users\Sugarfreeecho\.codex\sessions\2026\06`)

---

## 6月1日

**概要**: 修改 14 个文件（共 14 个文件，66 次变更）

| 文件路径 | 类型 | 次数 | 变更说明 |
|----------|------|------|----------|
| `app\agent_harness.py` | 修改 | 3 | +def _remove_subagent_parent_rows(self, parent_session_id: str, child_session_id: str) -> None:; ... |
| `app\agent_loop.py` | 修改 | 3 | +def _persist_state(state: State):; """实时保存当前会话的所有状态到磁盘。"""; _persist_session_messages(state) | -... |
| `app\agent_tokenizer.py` | 修改 | 1 | -tools_body = load_prompt_template("tools_description").strip(); "## 内置工具说明\n\n" + tools_body, |
| `app\agent_tools.py` | 修改 | 2 | +def activate_skill(skill_name: str) -> str:; result_parts.append(""); result_parts.append("### I... |
| `app\path_picker_util.py` | 修改 | 7 | +def _ps_quote(s: str) -> str:; return "'" + str(s or "").replace("'", "''") + "'"; def _pick_win... |
| `app\templates\index.html` | 修改 | 2 | +(function bindSessionMenuDocumentCloserOnce() {; if (window.__myAgentSessionMenuCloser) return;;... |
| `app\templates\static\myagent_path_picker.js` | 修改 | 2 | +var controller = (typeof AbortController !== 'undefined') ? new AbortController() : null;; var t... |
| `app\webui.py` | 修改 | 3 | +仅保留 ui_events[0:before_index]（下标 before_index 及之后丢弃），; 并重建主对话/上下文。使用 query 传参，避免 form 解析失败。; if ... |
| `frontend\src\app\legacy-ui.js` | 修改 | 34 | +(function bindSessionListDelegatedSwitcherOnce() {; if (!sessionsList || window.__myAgentSession... |
| `frontend\src\app\modules\message-rendering.js` | 修改 | 2 | +if (!Number.isFinite(Number(beforeIndex)) || Number(beforeIndex) <= 0) {; return { ok: false, er... |
| `frontend\src\app\modules\session-management.js` | 修改 | 2 | +(function bindSessionListDelegatedSwitcherOnce() {; if (!sessionsList || window.__myAgentSession... |
| `frontend\src\app\modules\shared-state-and-dialogs.js` | 修改 | 2 | +let todoRefreshEpoch = 0; |
| `frontend\src\app\modules\sse-handling.js` | 修改 | 1 | +else if (parsed.type === 'status') {; var statusContent = String(parsed.content || '');; var isT... |
| `frontend\src\vendor\myagent_path_picker.js` | 修改 | 2 | +var controller = (typeof AbortController !== 'undefined') ? new AbortController() : null;; var t... |

### 详细变更

#### ⏱ 00:10 会话（共 66 次补丁）

- **修改** `app\agent_harness.py`
  - +def _remove_subagent_parent_rows(self, parent_session_id: str, child_session_id: str) -> None:; child_id = str(child_session_id or "").strip(); if ... | -def _remove_subagent_parent_rows(self, parent_session_id: str, child_session_id: str) -> None:; child_id = str(child_session_id or "").strip(); if ...
- **修改** `app\agent_loop.py`
  - +def _persist_state(state: State):; """实时保存当前会话的所有状态到磁盘。"""; _persist_session_messages(state) | -def _persist_state(state: State):; """实时保存当前会话的所有状态到磁盘。"""; _persist_session_messages(state)
- **修改** `app\agent_tokenizer.py`
  - -tools_body = load_prompt_template("tools_description").strip(); "## 内置工具说明\n\n" + tools_body,
- **修改** `app\agent_tools.py`
  - +def activate_skill(skill_name: str) -> str:; result_parts.append(""); result_parts.append("### Instructions") | -def activate_skill(skill_name: str) -> str:; result_parts.append(""); result_parts.append("### Instructions")
- **修改** `app\path_picker_util.py`
  - +def _ps_quote(s: str) -> str:; return "'" + str(s or "").replace("'", "''") + "'"; def _pick_windows_powershell( | -single = pick_native_path("file", initial, multiple=False); return [single] if single else []
- **修改** `app\templates\index.html`
  - +(function bindSessionMenuDocumentCloserOnce() {; if (window.__myAgentSessionMenuCloser) return;; window.__myAgentSessionMenuCloser = true; | -(function bindSessionMenuDocumentCloserOnce() {; if (window.__myAgentSessionMenuCloser) return;; window.__myAgentSessionMenuCloser = true;
- **修改** `app\templates\static\myagent_path_picker.js`
  - +var controller = (typeof AbortController !== 'undefined') ? new AbortController() : null;; var timer = controller ? setTimeout(function () { contro... | -var r = await fetch('/api/pick-path', {; method: 'POST',; headers: { 'Content-Type': 'application/json' },
- **修改** `app\webui.py`
  - +仅保留 ui_events[0:before_index]（下标 before_index 及之后丢弃），; 并重建主对话/上下文。使用 query 传参，避免 form 解析失败。; if int(before_index) <= 0: | -仅保留 ui_events[0:before_index]（下标 before_index 及之后丢弃），; 并重建主对话/上下文。使用 query 传参，避免 form 解析失败。; ok = session_manager.truncate_session_at_event_index(s...
- **修改** `frontend\src\app\legacy-ui.js`
  - +(function bindSessionListDelegatedSwitcherOnce() {; if (!sessionsList || window.__myAgentSessionListSwitcher) return;; window.__myAgentSessionListS... | -})();; /**
- **修改** `frontend\src\app\modules\message-rendering.js`
  - +if (!Number.isFinite(Number(beforeIndex)) || Number(beforeIndex) <= 0) {; return { ok: false, error: 'invalid_before_index' };; function normalizeB... | -async function waitForBranchFinalPersisted(sessionId, beforeIndex) {; if (!sessionId || !Number.isFinite(beforeIndex) || beforeIndex <= 0) return t...
- **修改** `frontend\src\app\modules\session-management.js`
  - +(function bindSessionListDelegatedSwitcherOnce() {; if (!sessionsList || window.__myAgentSessionListSwitcher) return;; window.__myAgentSessionListS... | -switchSession(sess.id);; wsEl.addEventListener('click', function () { switchSession(sess.id); });; if (!nameSpan.classList.contains('editing')) swi...
- **修改** `frontend\src\app\modules\shared-state-and-dialogs.js`
  - +let todoRefreshEpoch = 0;
- **修改** `frontend\src\app\modules\sse-handling.js`
  - +else if (parsed.type === 'status') {; var statusContent = String(parsed.content || '');; var isTemporaryStatus = statusContent.indexOf('正在思考中...') ...
- **修改** `frontend\src\vendor\myagent_path_picker.js`
  - +var controller = (typeof AbortController !== 'undefined') ? new AbortController() : null;; var timer = controller ? setTimeout(function () { contro... | -var r = await fetch('/api/pick-path', {; method: 'POST',; headers: { 'Content-Type': 'application/json' },

---

## 6月2日

**概要**: 新增 3 个文件，修改 15 个文件（共 18 个文件，64 次变更）

| 文件路径 | 类型 | 次数 | 变更说明 |
|----------|------|------|----------|
| `RUN.bat` | 修改 | 1 | +set "ROOT=%~dp0"; cd /d "%ROOT%"; set "PYTHONIOENCODING=utf-8" | -set "ROOT=%~dp0"; set "PYTHON_... |
| `app\agent_harness.py` | 修改 | 6 | +except Exception as e:; logging.getLogger(__name__).warning("Failed to load encrypted config def... |
| `app\agent_loop.py` | 修改 | 13 | +session_id = sid; key_context = session_manager._load_key_context(session_id); key_context = ses... |
| `app\agent_openai.py` | 修改 | 8 | +OPENAI_MAX_RETRIES = max(1, int(os.getenv("OPENAI_MAX_RETRIES", "3"))); OPENAI_RETRY_BASE_SEC = ... |
| `app\agent_tools.py` | 修改 | 11 | +killer = await asyncio.create_subprocess_exec(; "taskkill",; "/PID", | -await asyncio.create_sub... |
| `app\webui.py` | 修改 | 1 | +_PATH_PICKER_JS_PATH = Path(__file__).resolve().parent / "templates" / "static" / "myagent_path_... |
| `frontend\README.md` | 新增/修改 | 3 | +│   │   ├── index.js        # UI 启动器; │   │   ├── modules/        # 按功能拆分的 UI 模块; UI 逻辑已按功能拆到 `s... |
| `frontend\src\app\chunks\01-settings-state.js` | 修改 | 1 | +var envAdv = document.getElementById('settings-env-advanced');; if (envAdv) {; envAdv.addEventLi... |
| `frontend\src\app\index.js` | 新增 | 1 | - |
| `frontend\src\app\legacy-ui.js` | 修改 | 1 | +var envAdv = document.getElementById('settings-env-advanced');; if (envAdv) {; envAdv.addEventLi... |
| `frontend\src\app\modules\chat.js` | 修改 | 5 | +import {; appendLogVisible,; applyCacheStatsFromEvent, | -import { appendLogVisible } from './co... |
| `frontend\src\app\modules\core-utils.js` | 修改 | 2 | +function updateSessionTitle() {; const br = document.getElementById('breadcrumb-text');; const s... |
| `frontend\src\app\modules\message.js` | 修改 | 2 | +createProcessFeedRow,; removeMessagesFromNode,; onMessageToolbarClick, | -createProcessFeedRow,;... |
| `frontend\src\app\modules\subagent.js` | 修改 | 3 | +subagentCardLoadQueue.length = 0;; $.subagentCardLoadInflight = 0;; Object.keys(subagentCardLoad... |
| `frontend\src\app\modules\toc-todo.js` | 修改 | 1 | +import {; bindUiHoverTip,; hideUiHoverTooltip, | -import {; bindUiHoverTip,; hideUiHoverTooltip, |
| `frontend\src\app\ui-runner.js` | 新增 | 1 | - |
| `frontend\src\main.js` | 修改 | 3 | +import './styles/main.css';; import './vendor/myagent_path_picker.js';; import './app/legacy-ui.... |
| `frontend\vite.config.js` | 修改 | 1 | +'/sessions': { target: 'http://127.0.0.1:8192', changeOrigin: true },; '/api': { target: 'http:/... |

### 详细变更

#### ⏱ 09:35 会话（共 21 次补丁）

- **修改** `RUN.bat`
  - +set "ROOT=%~dp0"; cd /d "%ROOT%"; set "PYTHONIOENCODING=utf-8" | -set "ROOT=%~dp0"; set "PYTHON_EXE=%ROOT%python\python.exe"
- **新增** `frontend\README.md`
- **修改** `frontend\src\app\chunks\01-settings-state.js`
  - +var envAdv = document.getElementById('settings-env-advanced');; if (envAdv) {; envAdv.addEventListener('click', function () { | -var envAdv = document.getElementById('settings-env-advanced');; if (envAdv) {; envAdv.addEventListener('click', function () {
- **修改** `frontend\src\app\legacy-ui.js`
  - +var envAdv = document.getElementById('settings-env-advanced');; if (envAdv) {; envAdv.addEventListener('click', function () { | -var envAdv = document.getElementById('settings-env-advanced');; if (envAdv) {; envAdv.addEventListener('click', function () {
- **修改** `frontend\src\app\modules\chat.js`
  - +import {; appendLogVisible,; applyCacheStatsFromEvent, | -import { appendLogVisible } from './core-utils.js';
- **修改** `frontend\src\app\modules\core-utils.js`
  - +function updateSessionTitle() {; const br = document.getElementById('breadcrumb-text');; const sub = document.getElementById('breadcrumb-sub'); | -function updateSessionTitle() {; const br = document.getElementById('breadcrumb-text');; const sub = document.getElementById('breadcrumb-sub');
- **修改** `frontend\src\app\modules\message.js`
  - +createProcessFeedRow,; removeMessagesFromNode,; onMessageToolbarClick, | -createProcessFeedRow,; onMessageToolbarClick,
- **修改** `frontend\src\app\modules\subagent.js`
  - +subagentCardLoadQueue.length = 0;; $.subagentCardLoadInflight = 0;; Object.keys(subagentCardLoadQueued).forEach(function (key) { | -subagentCardLoadQueue = [];; $.subagentCardLoadInflight = 0;; subagentCardLoadQueued = Object.create(null);
- **修改** `frontend\src\app\modules\toc-todo.js`
  - +import {; bindUiHoverTip,; hideUiHoverTooltip, | -import {; bindUiHoverTip,; hideUiHoverTooltip,
- **新增** `frontend\src\app\ui-runner.js`
- **修改** `frontend\src\main.js`
  - +import './styles/main.css';; import './vendor/myagent_path_picker.js';; import './app/legacy-ui.js'; | -import './styles/main.css';; import './vendor/myagent_path_picker.js';; import { $ } from './app/modules/state.js';
- **修改** `frontend\vite.config.js`
  - +'/sessions': { target: 'http://127.0.0.1:8192', changeOrigin: true },; '/api': { target: 'http://127.0.0.1:8192', changeOrigin: true }, | -'/sessions': { target: 'http://127.0.0.1:8000', changeOrigin: true },; '/api': { target: 'http://127.0.0.1:8000', changeOrigin: true },

#### ⏱ 12:19 会话（共 4 次补丁）

- **修改** `app\webui.py`
  - +_PATH_PICKER_JS_PATH = Path(__file__).resolve().parent / "templates" / "static" / "myagent_path_picker.js"; _TEMPLATES_DIR = Path(__file__).resolve... | -_PATH_PICKER_JS_PATH = Path(__file__).resolve().parent / "templates" / "static" / "myagent_path_picker.js"; _TEMPLATES_DIR = Path(__file__).resolve...
- **新增/修改** `frontend\README.md`
  - +│   │   ├── index.js        # UI 启动器; │   │   ├── modules/        # 按功能拆分的 UI 模块; UI 逻辑已按功能拆到 `src/app/modules/*`。后续可以继续把共享闭包逐步收敛成显式 `import`/`expo... | -│   │   ├── legacy-ui.js    # 原 index.html 内联 JS（待逐步拆模块）; 按功能把 `legacy-ui.js` 拆到 `src/features/*`、`src/state/*`、`src/api/*`，用 `import`/`export` 替代全...
- **新增** `frontend\src\app\index.js`
- **修改** `frontend\src\main.js`
  - +import './app/index.js'; | -import './app/legacy-ui.js';

#### ⏱ 19:28 会话（共 38 次补丁）

- **修改** `app\agent_harness.py`
  - +except Exception as e:; logging.getLogger(__name__).warning("Failed to load encrypted config defaults: %s", e); except Exception as e: | -except Exception:; except:; except Exception:
- **修改** `app\agent_loop.py`
  - +session_id = sid; key_context = session_manager._load_key_context(session_id); key_context = session_manager.migrate_todo_plan_off_key_context(sess... | -session_id, _, work_messages_dicts, llm_history_dicts, key_context, _metadata = (; session_manager.get_or_create_session(sid)
- **修改** `app\agent_openai.py`
  - +OPENAI_MAX_RETRIES = max(1, int(os.getenv("OPENAI_MAX_RETRIES", "3"))); OPENAI_RETRY_BASE_SEC = float(os.getenv("OPENAI_RETRY_BASE_SEC", "1.0")); d... | -OPENAI_MAX_RETRIES = max(1, int(os.getenv("OPENAI_MAX_RETRIES", "3"))); OPENAI_RETRY_BASE_SEC = float(os.getenv("OPENAI_RETRY_BASE_SEC", "1.0")); d...
- **修改** `app\agent_tools.py`
  - +killer = await asyncio.create_subprocess_exec(; "taskkill",; "/PID", | -await asyncio.create_subprocess_exec(; "taskkill",; "/PID",

---

## 6月3日

**概要**: 修改 8 个文件（共 8 个文件，33 次变更）

| 文件路径 | 类型 | 次数 | 变更说明 |
|----------|------|------|----------|
| `app\agent_harness.py` | 修改 | 5 | +def _save_metadata(self, session_id: str, metadata: dict) -> None:; with self._session_metadata_... |
| `app\agent_mcp.py` | 修改 | 6 | +def _register_tools_globally(alias: str, tools: List[Any]) -> int:; """Register a server's lates... |
| `app\templates\dist\assets\index-CSwagtx-.js` | 修改 | 10 | +let sessionListLoadEpoch = 0;; let createNewSessionQueue = Promise.resolve(); |
| `app\templates\dist\assets\index-CodexArchiveLazy.js` | 修改 | 2 | +var expanded = optsSection.forceExpanded ? true : sessionSectionExpanded(sectionKey); | -var exp... |
| `app\templates\dist\assets\index-CodexSessionFix.js` | 修改 | 2 | +let preCount = await getUiEventCount(submitSessionId);; if (!Number.isFinite(preCount) || preCou... |
| `app\templates\dist\assets\index-CodexTocFix.js` | 修改 | 3 | +let archivedSessionsLoaded = false; |
| `app\templates\dist\index.html` | 修改 | 4 | +<script type="module" crossorigin src="/assets/index-CodexSessionFix.js"></script> | -<script ty... |
| `app\webui.py` | 修改 | 1 | +async def create_session():; # get_or_create_session 现在返回6个值，我们只需要 session_id; session_id, _, _,... |

### 详细变更

#### ⏱ 12:45 会话（共 33 次补丁）

- **修改** `app\agent_harness.py`
  - +def _save_metadata(self, session_id: str, metadata: dict) -> None:; with self._session_metadata_lock(session_id):; self._save_metadata_unlocked(ses... | -def _save_metadata(self, session_id: str, metadata: dict) -> None:; with self._session_metadata_lock(session_id):; self._save_metadata_unlocked(ses...
- **修改** `app\agent_mcp.py`
  - +def _register_tools_globally(alias: str, tools: List[Any]) -> int:; """Register a server's latest tools in the global OpenAI tool snapshots."""; gl...
- **修改** `app\templates\dist\assets\index-CSwagtx-.js`
  - +let sessionListLoadEpoch = 0;; let createNewSessionQueue = Promise.resolve();
- **修改** `app\templates\dist\assets\index-CodexArchiveLazy.js`
  - +var expanded = optsSection.forceExpanded ? true : sessionSectionExpanded(sectionKey); | -var expanded = sessionSectionExpanded(sectionKey);; if (sectionKey === 'archived' && isExpanded && !archivedSessionsLoaded) {; void loadSessions({ ...
- **修改** `app\templates\dist\assets\index-CodexSessionFix.js`
  - +let preCount = await getUiEventCount(submitSessionId);; if (!Number.isFinite(preCount) || preCount < 0) {; preCount = uiEventCountCache.get(submitS... | -const preCount = uiEventCountCache.get(submitSessionId);; getUiEventCount(submitSessionId).then(function(serverCount) {; uiEventCountCache.updateFr...
- **修改** `app\templates\dist\assets\index-CodexTocFix.js`
  - +let archivedSessionsLoaded = false;
- **修改** `app\templates\dist\index.html`
  - +<script type="module" crossorigin src="/assets/index-CodexSessionFix.js"></script> | -<script type="module" crossorigin src="/assets/index-CSwagtx-.js"></script>
- **修改** `app\webui.py`
  - +async def create_session():; # get_or_create_session 现在返回6个值，我们只需要 session_id; session_id, _, _, _, _, _ = session_manager.get_or_create_session() | -async def create_session():; # get_or_create_session 现在返回6个值，我们只需要 session_id; session_id, _, _, _, _, _ = session_manager.get_or_create_session()

---

## 6月5日

**概要**: 新增 6 个文件，修改 7 个文件，删除 3 个文件（共 13 个文件，71 次变更）

| 文件路径 | 类型 | 次数 | 变更说明 |
|----------|------|------|----------|
| `RUN.bat` | 新增/修改 | 8 | +echo                Agent Tray Launcher | -echo                Agent  托盘启动器 |
| `RUN_CONSOLE.bat` | 新增/修改/删除 | 3 | +echo                Agent Console Launcher | -echo                Agent  智能会话助手 |
| `RUN_TRAY.bat` | 新增/修改/删除 | 3 | +set "PYTHON_EXE=%ROOT%python\pythonw.exe"; start "" "%PYTHON_EXE%" "%LAUNCHER_PY%" | -set "PYTHO... |
| `app\agent_loop.py` | 修改 | 4 | +finally:; if task.done():; await task | -finally:; await task; except asyncio.CancelledError: |
| `app\colored_log_viewer.ps1` | 新增 | 1 | - |
| `app\session_event_bus.py` | 新增/修改 | 3 | +elif event.get("type") == "tool_call":; _prune_recent_ephemeral_unlocked(; types={"tool_pending"... |
| `app\session_lifecycle.py` | 修改 | 1 | +def is_run_active(session_id: str) -> bool:; sid = (session_id or "").strip(); if not sid: |
| `app\tray_launcher.py` | 新增/修改/删除 | 24 | +_CONSOLE_CTRL_HANDLER = None; def install_console_close_to_tray_handler(console_hwnd: int) -> No... |
| `app\webui.py` | 修改 | 5 | +from agent import astream_events, astream_events_continuation, session_manager; from agent_harne... |
| `frontend\src\app\modules\message-rendering.js` | 修改 | 7 | +function workspaceRelativePathNoSlashAutoLinkOk(relPath) {; var t = linkifyNormalizePathToken(St... |
| `frontend\src\app\modules\session-management.js` | 修改 | 2 | +if (!run) {; setSendButtonState();; syncSessionListIndicatorClasses(); | -if (!run) return;; con... |
| `frontend\src\app\modules\session-scroll-history.js` | 修改 | 6 | +function isSessionRunning(sessionId) {; return !!(sessionId && (runningBySession[sessionId] || s... |
| `frontend\src\app\modules\sse-handling.js` | 修改 | 4 | +async function attachSessionEventStream(sessionId) {; if (!sessionId || runningBySession[session... |

### 详细变更

#### ⏱ 16:37 会话（共 14 次补丁）

- **新增/修改** `RUN.bat`
  - +echo                Agent Tray Launcher | -echo                Agent  托盘启动器
- **新增/删除/修改** `RUN_CONSOLE.bat`
  - +echo                Agent Console Launcher | -echo                Agent  智能会话助手
- **新增/删除/修改** `RUN_TRAY.bat`
  - +set "PYTHON_EXE=%ROOT%python\pythonw.exe"; start "" "%PYTHON_EXE%" "%LAUNCHER_PY%" | -set "PYTHON_EXE=%ROOT%python\python.exe"; "%PYTHON_EXE%" "%LAUNCHER_PY%"; if errorlevel 1 pause
- **新增/删除/修改** `app\tray_launcher.py`
  - +_CONSOLE_CTRL_HANDLER = None; def install_console_close_to_tray_handler(console_hwnd: int) -> None:; if not console_hwnd:
- **修改** `frontend\src\app\modules\message-rendering.js`
  - +function workspaceRelativePathNoSlashAutoLinkOk(relPath) {; var t = linkifyNormalizePathToken(String(relPath || '').trim());; if (!t || t.charAt(0)... | -if (parent.closest('code') && !isEntireTextNodeWindowsPath(raw) && !isEntireBareFilenameLinkable(raw) && !isEntireWorkspaceSlashPathLinkable(raw) &...

#### ⏱ 17:50 会话（共 42 次补丁）

- **新增/修改** `RUN.bat`
  - +chcp 65001 >nul; set "ROOT=%~dp0" | -chcp 65001 >nul; echo ==================================================; echo                Agent WebUI Launcher
- **修改** `app\agent_loop.py`
  - +finally:; if task.done():; await task | -finally:; await task; except asyncio.CancelledError:
- **新增** `app\colored_log_viewer.ps1`
- **新增/修改** `app\session_event_bus.py`
  - +elif event.get("type") == "tool_call":; _prune_recent_ephemeral_unlocked(; types={"tool_pending", "tool_call_delta", "tool_command_delta"},
- **修改** `app\session_lifecycle.py`
  - +def is_run_active(session_id: str) -> bool:; sid = (session_id or "").strip(); if not sid:
- **新增/修改** `app\tray_launcher.py`
  - +import win32event; import winerror; self.mutex = win32event.CreateMutex(None, True, "MyAgentTrayLauncher") | -self._start_agent()
- **修改** `app\webui.py`
  - +from agent import astream_events, astream_events_continuation, session_manager; from agent_harness import PROJECT_ROOT, WORK_DIR, dotenv_file_path,... | -from agent import astream_events, astream_events_continuation, session_manager; from agent_harness import PROJECT_ROOT, WORK_DIR, dotenv_file_path,...
- **修改** `frontend\src\app\modules\message-rendering.js`
  - +function describeServerSyncFailure(res, fallback) {; var base = fallback || '无法同步服务器。';; var err = res && res.error ? String(res.error).trim() : '';
- **修改** `frontend\src\app\modules\session-management.js`
  - +if (!run) {; setSendButtonState();; syncSessionListIndicatorClasses(); | -if (!run) return;; const ctx = run.ctx;
- **修改** `frontend\src\app\modules\session-scroll-history.js`
  - +function isSessionRunning(sessionId) {; return !!(sessionId && (runningBySession[sessionId] || serverStreamActiveBySession[sessionId])); | -function isSessionRunning(sessionId) { return !!(sessionId && runningBySession[sessionId]); }
- **修改** `frontend\src\app\modules\sse-handling.js`
  - +async function attachSessionEventStream(sessionId) {; if (!sessionId || runningBySession[sessionId]) return;; if (!serverStreamActiveBySession[sess...

---

## 6月7日

**概要**: 新增 4 个文件，修改 12 个文件（共 16 个文件，27 次变更）

| 文件路径 | 类型 | 次数 | 变更说明 |
|----------|------|------|----------|
| `.gitattributes` | 新增 | 1 | - |
| `.gitignore` | 新增 | 1 | - |
| `FRONTEND_WORKFLOW.md` | 新增 | 1 | - |
| `SPEC.md` | 新增 | 4 | - |
| `app\agent_harness.py` | 修改 | 4 | +def consume_pending_subagent_notifications(self, session_id: str) -> List[str]:; """读取并消费可注入的后台 ... |
| `app\agent_loop.py` | 修改 | 1 | +strip_reasoning_for_api_request,; resolve_executor_for_session,; EXECUTOR_REASONING_EFFORT, | -s... |
| `app\agent_memory.py` | 修改 | 4 | +hint_sink: Optional[Callable[[Any], None]] = None,; hints: Optional[List[str]] = None,; session_... |
| `app\agent_openai.py` | 修改 | 1 | +主模型在思考开时由 harness 传 extra_body.thinking、reasoning_effort，并继续传 temperature；; messages_to_openai_p... |
| `app\agent_subagent.py` | 修改 | 1 | +if not body:; preview = ""; for n in session_manager.list_subagents_flat( | -if not body:; previ... |
| `app\templates\dist\assets\index-CKpTjyZA.js` | 修改 | 2 | +let archivedSessionsLoaded = false;; let archivedSessionsCache = null;; const response = await f... |
| `app\templates\dist\assets\index-DG5GA9jP.css` | 修改 | 1 | +.session-archive-load-btn{width:calc(100% - .9rem);margin:.18rem .45rem .35rem;padding:.46rem .6... |
| `app\templates\dist\index.html` | 修改 | 1 | +<script type="module" crossorigin src="/assets/index-CodexArchiveLoad.js"></script>; <link rel="... |
| `app\webui.py` | 修改 | 1 | +async def list_sessions(include_archived: bool = Query(False)):; sessions = session_manager.list... |
| `frontend\README.md` | 修改 | 1 | +## Source of Truth; `frontend/src/` is the source of truth for the UI. Do not hand-edit files un... |
| `frontend\src\app\modules\session-management.js` | 修改 | 2 | +sessionListCache.invalidate();; sessionListCache.invalidate();; archivedSessionsLoaded = false; ... |
| `frontend\src\styles\app.css` | 修改 | 1 | +.session-section.is-collapsed .session-section-chev { transform: rotate(-90deg); }; .session-sec... |

### 详细变更

#### ⏱ 05:50 会话（共 1 次补丁）

- **新增** `SPEC.md`

#### ⏱ 05:57 会话（共 1 次补丁）

- **新增** `SPEC.md`

#### ⏱ 05:57 会话（共 1 次补丁）

- **新增** `SPEC.md`

#### ⏱ 07:56 会话（共 1 次补丁）

- **新增** `SPEC.md`

#### ⏱ 08:01 会话（共 20 次补丁）

- **新增** `.gitattributes`
- **新增** `.gitignore`
- **新增** `FRONTEND_WORKFLOW.md`
- **修改** `app\agent_harness.py`
  - +def consume_pending_subagent_notifications(self, session_id: str) -> List[str]:; """读取并消费可注入的后台 subagent 通知，供父 react_node 注入。"""; with open(path, "... | -def consume_pending_subagent_notifications(self, session_id: str) -> List[str]:; """读取并消费可注入的后台 subagent 通知，供父 react_node 注入。"""; with open(path, "...
- **修改** `app\agent_loop.py`
  - +strip_reasoning_for_api_request,; resolve_executor_for_session,; EXECUTOR_REASONING_EFFORT, | -strip_reasoning_for_api_request,; resolve_executor_for_session,; EXECUTOR_REASONING_EFFORT,
- **修改** `app\agent_memory.py`
  - +hint_sink: Optional[Callable[[Any], None]] = None,; hints: Optional[List[str]] = None,; session_id: str = "",
- **修改** `app\agent_openai.py`
  - +主模型在思考开时由 harness 传 extra_body.thinking、reasoning_effort，并继续传 temperature；; messages_to_openai_params 对每条 assistant 均带上 reasoning_content（可空串），兼容 D... | -主模型在思考开时由 harness 传 extra_body.thinking、reasoning_effort，且省略 temperature；; messages_to_openai_params 对每条 assistant 均带上 reasoning_content（可空串），兼容 De...
- **修改** `app\agent_subagent.py`
  - +if not body:; preview = ""; for n in session_manager.list_subagents_flat( | -if not body:; preview = ""; for n in session_manager.list_subagents_flat(
- **修改** `app\templates\dist\assets\index-CKpTjyZA.js`
  - +let archivedSessionsLoaded = false;; let archivedSessionsCache = null;; const response = await fetch('/sessions'); | -const response = await fetch('/sessions?include_archived=true');; const archivedList = [];; if (arch) archivedList.push(s);
- **修改** `app\templates\dist\assets\index-DG5GA9jP.css`
  - +.session-archive-load-btn{width:calc(100% - .9rem);margin:.18rem .45rem .35rem;padding:.46rem .6rem;border:1px solid var(--border-glass);border-rad...
- **修改** `app\templates\dist\index.html`
  - +<script type="module" crossorigin src="/assets/index-CodexArchiveLoad.js"></script>; <link rel="stylesheet" crossorigin href="/assets/index-CodexAr... | -<script type="module" crossorigin src="/assets/index-CKpTjyZA.js"></script>; <link rel="stylesheet" crossorigin href="/assets/index-DG5GA9jP.css">
- **修改** `app\webui.py`
  - +async def list_sessions(include_archived: bool = Query(False)):; sessions = session_manager.list_sessions(include_archived=include_archived); archi... | -async def list_sessions(include_archived: bool = Query(False)):; sessions = session_manager.list_sessions(include_archived=include_archived); _clea...
- **修改** `frontend\README.md`
  - +## Source of Truth; `frontend/src/` is the source of truth for the UI. Do not hand-edit files under; `app/templates/dist/`; that directory is gener...
- **修改** `frontend\src\app\modules\session-management.js`
  - +sessionListCache.invalidate();; sessionListCache.invalidate();; archivedSessionsLoaded = false; | -const response = await fetch('/sessions?include_archived=true');; const archivedList = [];; if (arch) archivedList.push(s);
- **修改** `frontend\src\styles\app.css`
  - +.session-section.is-collapsed .session-section-chev { transform: rotate(-90deg); }; .session-section.is-collapsed .session-section-body { display: ... | -.session-section.is-collapsed .session-section-chev { transform: rotate(-90deg); }; .session-section.is-collapsed .session-section-body { display: ...

---

## 📊 统计摘要

| 指标 | 数值 |
|------|------|
| 活跃天数 | 5 天 |
| 会话总数 | 12 |
| 补丁总数 | 242 |
| 涉及文件数 | 49 |

### 🔥 修改频率 Top 15

| 文件 | 修改次数 |
|------|----------|
| `frontend\src\app\legacy-ui.js` | 35 |
| `app\tray_launcher.py` | 24 |
| `app\agent_loop.py` | 21 |
| `app\agent_harness.py` | 18 |
| `app\agent_tools.py` | 13 |
| `app\webui.py` | 11 |
| `app\templates\dist\assets\index-CSwagtx-.js` | 10 |
| `frontend\src\app\modules\message-rendering.js` | 9 |
| `RUN.bat` | 9 |
| `app\agent_openai.py` | 9 |
| `app\path_picker_util.py` | 7 |
| `frontend\src\app\modules\session-management.js` | 6 |
| `app\agent_mcp.py` | 6 |
| `frontend\src\app\modules\session-scroll-history.js` | 6 |
| `frontend\src\app\modules\sse-handling.js` | 5 |

### 📁 文件分类

- **其他**: 2 个文件
- **前端**: 19 个文件
- **后端**: 23 个文件
- **文档**: 2 个文件
- **脚本**: 3 个文件
